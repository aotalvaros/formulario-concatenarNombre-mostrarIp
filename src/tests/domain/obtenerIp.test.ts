import { obtenerIp } from '../../domain/obtenerIp';
import { mockFunction } from '../../helpers/JestHelpers';

jest.mock('../../domain/obtenerIp');

describe('Obtener IP', () => {

    test('debe devolver una direccio ip', async() => {     
             
    });
    
});
